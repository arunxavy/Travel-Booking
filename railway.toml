[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "npm run db:migrate --workspace=server && node server/dist/index.js"
healthcheckPath = "/health"
restartPolicyType = "on_failure"
